% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{.save_output}
\alias{.save_output}
\title{Write the pipeline output to disk}
\usage{
.save_output(output, output_path, merge)
}
\arguments{
\item{output}{Either a single \code{data.table} (merged) or a named list
of \code{data.table}s (separate).}

\item{output_path}{File path (merged) or directory path (separate), or
\code{NULL} to skip writing.}

\item{merge}{Logical - matches the \code{merge} argument from
\code{\link[=run_pipeline]{run_pipeline()}}.}
}
\description{
Handles both the merged (\code{merge = TRUE}) and separate (\code{merge = FALSE})
cases:
\itemize{
\item Merged: writes a single CSV to \code{output_path}.
\item Separate: writes one CSV per dataset, using the dataset label as the
filename stem, into the directory given by \code{output_path}.
}
}
\details{
Does nothing when \code{output_path} is \code{NULL}.
}
\keyword{internal}
