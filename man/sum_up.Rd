% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sum_up.R
\name{sum_up}
\alias{sum_up}
\alias{summarise}
\alias{summarize}
\title{Summarise Observations}
\usage{
sum_up(dt, var, config, category = FALSE, digits = 1)

summarise(dt, var, config, category = FALSE, digits = 1)

summarize(dt, var, config, category = FALSE, digits = 1)
}
\arguments{
\item{dt}{A \code{data.table} or \code{data.frame}. If a \code{data.frame} is supplied,
it is converted internally to a \code{data.table}.}

\item{var}{Variable to sum_up. Unquoted column name.}

\item{config}{Configuration list containing \code{value_map}.}

\item{category}{Logical. If \code{TRUE}, convert the grouping variable to a
factor using labels from \code{config$value_map}.}

\item{digits}{Number of decimal places used when rounding percentages.}
}
\value{
A \code{data.table} containing:
\describe{
\item{var}{The grouping variable.}
\item{n}{Number of observations.}
\item{pct}{Percentage of observations.}
}
}
\description{
Counts the number of observations for a variable and calculates the
corresponding percentages. Optionally converts the grouping variable
to a factor using labels defined in the configuration object.
}
\details{
The function expects a configuration object containing a
\code{value_map} element, where each variable name corresponds to a lookup
table with columns \code{num_value} and \code{chr_value}.

If \code{category = TRUE} and the variable is of class
\code{haven_labelled}, labels are extracted using
\code{haven::as_factor(levels = "labels")}.

Otherwise, labels are taken from
\code{config$value_map[[var_name]]}, using:

\itemize{
\item \code{num_value} as factor levels
\item \code{chr_value} as factor labels
}
}
\examples{
library(data.table)

dt <- data.table(
  kjonn = c(1, 2, 1, 2, 1, 1, 2, 2, 1, 2)
)

cfg <- list(
  value_map = list(
    kjonn = data.table(
      num_value = c(1, 2),
      chr_value = c("Mann", "Kvinne")
    )
  )
)

sum_up(dt, kjonn, cfg)

sum_up(dt, kjonn, cfg, category = TRUE)

}
