% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_dataset.R
\name{.coerce_column}
\alias{.coerce_column}
\title{Step 7 (single column) - attempt safe type coercion}
\usage{
.coerce_column(x, target_type, var, dataset_label = "")
}
\arguments{
\item{x}{A vector (one column from the data.table).}

\item{target_type}{String: "integer", "numeric", "double", "character",
or "logical".}

\item{var}{Variable name (used in issue messages only).}

\item{dataset_label}{Dataset label for warning messages.}
}
\value{
A list:
\describe{
\item{converted}{The coerced vector, or \code{NULL} for unknown types.}
\item{success}{\code{TRUE} if coercion is safe (no new NAs).}
\item{issue}{A one-row data.table describing the problem, or \code{NULL}.}
}
}
\description{
Converts \code{x} to \code{target_type} and checks whether the conversion introduced
any \emph{new} \code{NA} values (i.e. values that were non-missing before but became
\code{NA} after casting).  Returns a list so the caller can decide whether to
apply the result or record an issue.
}
\details{
Separating the attempt from the application means \code{\link[=.coerce_all_columns]{.coerce_all_columns()}}
never modifies the table for a column that would silently lose data.
}
\keyword{internal}
