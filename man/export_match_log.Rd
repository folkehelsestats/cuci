% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_log.R
\name{export_match_log}
\alias{export_match_log}
\title{Export the column matching log to CSV files}
\usage{
export_match_log(
  match_result,
  issues_dt = NULL,
  dataset_label = "",
  year_tag = NA,
  log_dir = "logs/matching",
  append_master = TRUE
)
}
\arguments{
\item{match_result}{List returned by \code{\link[=match_columns]{match_columns()}}.}

\item{issues_dt}{\code{data.table} of issues from \code{\link[=clean_dataset]{clean_dataset()}}, or
\code{NULL} to skip the issues log.}

\item{dataset_label}{Human-readable label (used in filenames and metadata).}

\item{year_tag}{Integer year for traceability. Default \code{NA}.}

\item{log_dir}{Directory where CSV logs are written. Created if absent.}

\item{append_master}{If \code{TRUE} (default), updates the master log CSV.}
}
\value{
The annotated log \code{data.table} for this dataset, invisibly.
}
\description{
Takes the result of \code{\link[=match_columns]{match_columns()}} and writes two files: a per-dataset
match log and (optionally) an issues log from \code{\link[=clean_dataset]{clean_dataset()}}.  A master
log that accumulates entries across all datasets is also updated.
}
\details{
Each concern is handled by a dedicated internal helper so the function body
reads as a plain checklist of steps.
}
