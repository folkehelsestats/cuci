% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_dataset.R
\name{validate_dataset}
\alias{validate_dataset}
\title{Validate a cleaned survey dataset against the variable map}
\usage{
validate_dataset(dt, config = NULL, dataset_label = "")
}
\arguments{
\item{dt}{A cleaned \code{data.table} (output of \code{\link[=clean_dataset]{clean_dataset()}}).}

\item{config}{Config object from \code{\link[=load_config]{load_config()}}. Pass \code{NULL} to
skip value conformance checks.}

\item{dataset_label}{Optional string label for the report heading.}
}
\value{
\code{dt} invisibly, so the function can be used inline in a pipeline.
}
\description{
Runs structural checks (row count, column list, missingness) and - when a
config is supplied - value-conformance checks against the \verb{value:} blocks
in the YAML.  Each concern is handled by a dedicated internal helper so
individual checks can be tested and extended without touching the others.
}
\examples{
\dontrun{
yml <- system.file("extdata", "variable_map.yml", package = "cuci")
csv <- system.file("extdata", "test-data.csv",    package = "cuci")
cfg <- load_config(yml)
raw <- data.table::fread(csv)
out <- clean_dataset(raw, cfg)
validate_dataset(out$data, cfg, "test-data")
}

}
