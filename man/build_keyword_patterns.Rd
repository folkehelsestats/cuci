% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_columns.R
\name{build_keyword_patterns}
\alias{build_keyword_patterns}
\title{Build regex keyword patterns from a variable map config}
\usage{
build_keyword_patterns(config, min_char = 3)
}
\arguments{
\item{config}{List with a \verb{$var_map} element (from \code{\link[=load_config]{load_config()}}).}

\item{min_char}{Minimum keyword length to include. Default \code{3}.}
}
\value{
Named list of regex strings; variables without usable keywords are
omitted.
}
\description{
For each variable with a \verb{keywords:} block, produces a single regex that
matches any of those keywords as a substring not immediately surrounded by
other alphanumeric characters (so \code{kjonn} matches \code{io_kjonn} but not
\code{kjonnstudie}).
}
