% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_log.R
\name{.add_unmatched_rows}
\alias{.add_unmatched_rows}
\title{Append unmatched-column rows to the match log}
\usage{
.add_unmatched_rows(log_dt, match_result)
}
\arguments{
\item{log_dt}{\code{data.table} from \code{match_result$match_log}.}

\item{match_result}{Full result list from \code{\link[=match_columns]{match_columns()}}.}
}
\value{
A \code{data.table} with unmatched rows appended (or the original
\code{log_dt} if there were no unmatched columns).
}
\description{
Columns that fell through all three matching layers have no entry in
\code{match_result$match_log}.  This helper adds one row per unmatched column
so the audit log is a complete record of every column that was in the raw
dataset, including those that were dropped.
}
\keyword{internal}
