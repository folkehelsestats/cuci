% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_columns.R
\name{match_columns}
\alias{match_columns}
\title{Match raw column names to canonical variable names}
\usage{
match_columns(raw_colnames, config, fuzzy_max_distance = 0.15)
}
\arguments{
\item{raw_colnames}{Character vector of column names from the raw
dataset (after name normalisation).}

\item{config}{Config object returned by \code{\link[=load_config]{load_config()}}.}

\item{fuzzy_max_distance}{Numeric. Maximum edit distance for fuzzy matching.
Default \code{0.15}.}
}
\value{
A named list:
\describe{
\item{match_log}{\code{data.table} with one row per matched column.}
\item{rename_vec}{Named character vector \code{old -> canonical} for
\code{\link[data.table:setnames]{data.table::setnames()}}.}
\item{unmatched}{Column names that fell through all three layers.}
}
}
\description{
Thin orchestrator: passes column names through three matching layers in
descending confidence order (exact → fuzzy → keyword) and returns a
structured result.  Each layer is implemented as a separate internal helper
so individual layers can be tested or extended independently.
}
\examples{
yml <- system.file("extdata", "variable_map.yml", package = "cuci")
cfg <- load_config(yml)
match_columns(c("kjonn", "alder", "unknown_col"), cfg)$match_log

}
