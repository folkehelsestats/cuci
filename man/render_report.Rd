% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_report.R
\name{render_report}
\alias{render_report}
\title{Render Report from Matching Logs}
\usage{
render_report(
  log_dir = NULL,
  output_dir = NULL,
  output_file = NULL,
  open_browser = interactive(),
  create_dir = interactive()
)
}
\arguments{
\item{log_dir}{Character string. Directory containing matching log
files. Must include \code{match_log_MASTER.csv}.}

\item{output_dir}{Character string. Directory where the rendered HTML
report will be written.}

\item{output_file}{Character string. Filename for the rendered HTML
report. If \code{NULL}, a timestamped filename is generated automatically.}

\item{open_browser}{Logical. If \code{TRUE}, the rendered report is opened
in the default web browser upon successful rendering. Defaults to
\code{interactive()}.}

\item{create_dir}{Logical. Controls what happens if \code{output_dir} does
not exist. If \code{TRUE}, the directory is created automatically. If
\code{FALSE}, an error is thrown. The default is \code{interactive()}, which
asks for confirmation in an interactive session and throws an error
otherwise.}
}
\value{
Invisibly returns the path where the rendered report is
expected to be written.
}
\description{
Render a Quarto-based HTML audit report summarizing the results of a
column-matching workflow.
}
\details{
The report is generated from the bundled Quarto template
(\code{audit_report.qmd}) included with the package and uses the log files
located in \code{log_dir}.

A master log file named \code{match_log_MASTER.csv} must exist in
\code{log_dir}; otherwise the report cannot be rendered.

During rendering, the report is executed with
\code{execute_dir = getwd()}, allowing relative paths in the Quarto
document (for example \code{logs/matching}) to be resolved relative to the
current project directory.

The report template (\code{audit_report.qmd}) and associated assets are
distributed with the package under \verb{inst/quarto/}.

Quarto requires \code{output_file} to be a plain filename rather than a
path. For this reason a timestamped filename is generated during
rendering.
}
\examples{
\dontrun{
render_audit_report()

render_audit_report(
  log_dir = "logs/matching",
  output_dir = "reports"
)
}

}
\seealso{
\code{\link[quarto:quarto_render]{quarto::quarto_render()}}
}
