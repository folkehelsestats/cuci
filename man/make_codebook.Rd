% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{make_codebook}
\alias{make_codebook}
\alias{codebook}
\title{Create an interactive codebook using DT}
\usage{
make_codebook(d, config = NULL, page = 25, save = FALSE)

codebook(d, config = NULL, page = 25, save = FALSE)
}
\arguments{
\item{d}{A data frame, typically imported with \code{haven} or similar, where
variables have \code{label} and \code{labels} attributes.}

\item{config}{Path to a YAML config file. Needed if dataset has not been
imported with \code{haven} and value labels are not stored as attributes.}

\item{page}{Integer. Number of rows to display per page in the interactive
table. Default is 25.}

\item{save}{Logical value. Default is FALSE}
}
\value{
A \code{DT::datatable} object that can be rendered in R Markdown or Shiny
or a stand alone HTML file.
}
\description{
This function generates an interactive HTML table (codebook) for a dataset,
showing variable names, labels, and value labels extracted from attributes.
}
\details{
The value labels are displayed on separate lines using HTML \verb{<br>} tags.
To ensure these tags are rendered correctly, the argument \code{escape = FALSE}
is passed to \code{DT::datatable()}. Without this, HTML tags would be shown as
plain text instead of creating line breaks.

The table includes:
\itemize{
\item Column filters for easy searching.
\item Export buttons (Copy, CSV, Excel, PDF, Print).
}
}
\examples{
\dontrun{
library(haven)
df <- read_dta("yourfile.dta")
make_codebook(df)
}
}
