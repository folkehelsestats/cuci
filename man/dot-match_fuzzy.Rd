% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_columns.R
\name{.match_fuzzy}
\alias{.match_fuzzy}
\title{Layer 2 - fuzzy alias matching via Levenshtein edit distance}
\usage{
.match_fuzzy(candidates, name_lookup, claimed_canonical, max_distance = 0.15)
}
\arguments{
\item{candidates}{Character vector of unmatched column names.}

\item{name_lookup}{Named character vector from \code{config$name_lookup}.}

\item{claimed_canonical}{Character vector of canonical names already taken.}

\item{max_distance}{Passed to \code{agrep(max.distance=)}. Default 0.15.}
}
\value{
Same structure as \code{\link[=.match_exact]{.match_exact()}}.
}
\description{
Uses \code{\link[base:agrep]{base::agrep()}} to find aliases within \code{max_distance} edit-distance
of each candidate.  Takes the first hit (closest distance).  Columns that
already have an exact match are not passed in, so no overlap can occur.
}
\keyword{internal}
