% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_columns.R
\name{.find_matched_keyword}
\alias{.find_matched_keyword}
\title{Identify which plain keyword caused a regex hit}
\usage{
.find_matched_keyword(col, canonical, var_map)
}
\arguments{
\item{col}{The raw column name that was matched.}

\item{canonical}{The canonical variable name whose pattern fired.}

\item{var_map}{The raw parsed YAML list (\code{config$var_map}).}
}
\value{
A single string: the keyword that matched.
}
\description{
After \code{\link[=.match_keyword]{.match_keyword()}} confirms that a pattern matched, this function
looks up the \emph{plain} keyword list from \code{var_map} (not the compiled regex)
and tests each keyword individually with \code{fixed = TRUE}.  This avoids
splitting or re-parsing lookahead/lookbehind patterns for display purposes.
}
\details{
Returns the longest matching keyword, or \code{"(pattern)"} if none of the
individual keywords can be isolated (should not happen in practice).
}
\keyword{internal}
