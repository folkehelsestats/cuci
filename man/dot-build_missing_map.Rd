% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_config.R
\name{.build_missing_map}
\alias{.build_missing_map}
\title{Build the canonical -> missing-code vector map}
\usage{
.build_missing_map(var_map)
}
\arguments{
\item{var_map}{Parsed YAML list.}
}
\value{
Named list of integer vectors (or \code{NULL} per variable).
}
\description{
Reads the \verb{missing:} block for each variable and returns only the numeric
codes (e.g. \code{8}, \code{9}).  Literal YAML \code{~} / \code{NA} entries are stripped here
because those cells are already \code{NA} in R and need no recoding.
}
\details{
The result is used by \code{\link[=.apply_missing_codes]{.apply_missing_codes()}} when \code{apply_missing = TRUE}
is passed to \code{\link[=clean_dataset]{clean_dataset()}}.
}
\keyword{internal}
