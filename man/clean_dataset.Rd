% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_dataset.R
\name{clean_dataset}
\alias{clean_dataset}
\title{Clean and standardize a raw survey dataset}
\usage{
clean_dataset(
  dt,
  config,
  year_tag = NULL,
  dataset_label = "",
  apply_missing = FALSE
)
}
\arguments{
\item{dt}{A \code{data.table} (or coercible object).}

\item{config}{Config object returned by \code{\link[=load_config]{load_config()}}.}

\item{year_tag}{Optional integer year added as a \code{year} column.}

\item{dataset_label}{Human-readable label used in warnings and logs.}

\item{apply_missing}{If \code{TRUE}, recode numeric missing codes to \code{NA}.
Default \code{FALSE} (explicit opt-in to avoid silent data loss).}
}
\value{
A list:
\describe{
\item{data}{The cleaned \code{data.table}.}
\item{issues}{\code{data.table} with columns \code{variable}, \code{issue_type},
\code{detail} - one row per coercion failure or value violation.}
}
}
\description{
Orchestrates the full cleaning pipeline for a single dataset.  Each step
is delegated to a focused internal helper (see \code{clean_dataset.R} source)
so individual steps can be tested and extended independently.
}
\details{
Steps in order:
\enumerate{
\item Normalise column names (\code{\link[=.normalise_colnames]{.normalise_colnames()}})
\item Match columns to canonical names (\code{\link[=match_columns]{match_columns()}})
\item Apply renames (\code{\link[=.apply_renames]{.apply_renames()}})
\item Keep only canonical columns (\code{\link[=.select_canonical_cols]{.select_canonical_cols()}})
\item Recode values (\code{\link[=.apply_recodes]{.apply_recodes()}})
\item Optionally replace missing codes with NA (\code{\link[=.apply_missing_codes]{.apply_missing_codes()}})
\item Safe type coercion (\code{\link[=.coerce_all_columns]{.coerce_all_columns()}})
\item Value conformance validation (\code{\link[=.validate_all_values]{.validate_all_values()}})
\item Inject year column (\code{\link[=.inject_year]{.inject_year()}})
\item Remove duplicate/empty rows (\code{\link[=.drop_empty_rows]{.drop_empty_rows()}})
}
}
\examples{
\dontrun{
yml <- system.file("extdata", "variable_map.yml", package = "cuci")
csv <- system.file("extdata", "test-data.csv",    package = "cuci")
cfg <- load_config(yml)
raw <- data.table::fread(csv)
out <- clean_dataset(raw, cfg, year_tag = 2023, dataset_label = "test")
out$data
out$issues
}

}
