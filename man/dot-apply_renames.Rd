% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_dataset.R
\name{.apply_renames}
\alias{.apply_renames}
\title{Step 3 - rename matched columns to their canonical names}
\usage{
.apply_renames(dt, rename_vec)
}
\arguments{
\item{dt}{A data.table (modified by reference).}

\item{rename_vec}{Named character vector from \code{match_result$rename_vec}.}
}
\value{
\code{dt} invisibly (modification is in-place).
}
\description{
Applies \code{rename_vec} (a named character vector \code{old -> new}) to \code{dt} using
\code{\link[data.table:setnames]{data.table::setnames()}}.  Only renames entries where the old name actually
exists in \code{dt}; silently skips the rest so this function is safe to call
with a rename_vec built from a different dataset.
}
