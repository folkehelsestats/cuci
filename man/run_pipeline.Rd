% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run the full survey data cleaning pipeline}
\usage{
run_pipeline(
  file_manifest,
  config_file = "config/variable_map.yml",
  log_dir = "logs/matching",
  output_path = NULL,
  apply_missing = FALSE,
  merge = FALSE
)
}
\arguments{
\item{file_manifest}{A \code{data.table} or data.frame with columns:
\describe{
\item{path}{Path to each raw input file (CSV, TSV, or XLSX).}
\item{year_tag}{Integer year to attach, or \code{NA} if already in the file.}
}}

\item{config_file}{Path to the YAML variable map. Default
\code{"config/variable_map.yml"}.}

\item{log_dir}{Directory for audit logs. Default \code{"logs/matching"}.}

\item{output_path}{Where to write the output.  A file path when
\code{merge = TRUE}; a directory path when \code{merge = FALSE}.  Set \code{NULL} to
skip writing entirely.}

\item{apply_missing}{Passed to \code{\link[=clean_dataset]{clean_dataset()}}. Default \code{FALSE}.}

\item{merge}{Logical. If \code{TRUE}, stack all cleaned datasets into one
master \code{data.table}. If \code{FALSE} (default), return a named list of
separate \code{data.table}s, one per input file.}
}
\value{
\itemize{
\item When \code{merge = TRUE}: a single \code{data.table} with \code{year} as the first
column.
\item When \code{merge = FALSE}: a named list of \code{data.table}s, one per file,
keyed by the file's basename.
}
}
\description{
Processes every file in \code{file_manifest} through the complete pipeline
(read -> match -> log -> clean -> validate) and returns the results either
merged into one \code{data.table} or as a named list of separate \code{data.table}s.
}
\section{Output modes}{

\itemize{
\item \code{merge = TRUE} (default \code{FALSE}): stacks all cleaned datasets row-wise
into a single \code{data.table} using \code{\link[data.table:rbindlist]{data.table::rbindlist()}}.  Missing
columns across datasets are padded with \code{NA}.  \code{output_path} should be a
full file path (e.g. \code{"data/clean/master.csv"}).
\item \code{merge = FALSE}: returns a named list where each element is the cleaned
\code{data.table} for one file, keyed by the file's basename.  \code{output_path}
should be a directory (e.g. \code{"data/clean"}); one CSV per dataset is
written there.
}
}

\examples{
\dontrun{
library(data.table)
manifest <- data.table(
  path     = c(
    system.file("extdata", "survey_2022.csv", package = "cuci"),
    system.file("extdata", "survey_2023.csv", package = "cuci")
  ),
  year_tag = c(2022L, 2023L)
)
yml <- system.file("extdata", "variable_map.yml", package = "cuci")

# Return separate datasets (default)
result_list <- run_pipeline(manifest, config_file = yml, merge = FALSE)
result_list[["survey_2022.csv"]]

# Return one merged dataset
result_merged <- run_pipeline(manifest, config_file = yml, merge = TRUE,
                              output_path = "data/clean/master.csv")
}

}
