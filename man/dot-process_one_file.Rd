% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{.process_one_file}
\alias{.process_one_file}
\title{Run the full pipeline for a single raw data file}
\usage{
.process_one_file(path, year_tag, config, log_dir, apply_missing)
}
\arguments{
\item{path}{Path to the raw data file.}

\item{year_tag}{Integer year, or \code{NA} if already in the file.}

\item{config}{Config object from \code{\link[=load_config]{load_config()}}.}

\item{log_dir}{Directory for audit logs.}

\item{apply_missing}{Passed to \code{\link[=clean_dataset]{clean_dataset()}}.}
}
\value{
A list: \code{data} (cleaned \code{data.table}), \code{issues} (\code{data.table}),
\code{label} (filename used as dataset label).
}
\description{
Reads the file, runs column matching, exports the match log, cleans the
data, and validates the result.  Returns a list with the cleaned
\code{data.table} and the issues table from \code{\link[=clean_dataset]{clean_dataset()}}.
}
\keyword{internal}
